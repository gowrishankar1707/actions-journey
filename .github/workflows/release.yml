name: A fan-out fan-in workflow
on: push      
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: lint echo
        run: echo "this is lint"
    
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: unit-tests echo 
        run: echo "this is unit tests"
  package:
    runs-on: ubuntu-latest
    needs: [lint, unit-tests]
    steps:
      - name: unit-tests echo 
        run: echo "this is package"
